<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	<!-- 	根据ID查询用户信息 -->
	<select id="findById" parameterType="int" resultType="com.itheima.mybatis.pojo.User">
		SELECT
			`id`,
			`username`,
			`birthday`,
			`sex`,
			`address`
		FROM
			`USER`
		WHERE
			id = #{id}
	</select>
	
	<!-- 	根据用户姓名查询用户信息 -->
	<select id="findByUsername" parameterType="String" resultType="com.itheima.mybatis.pojo.User">
		SELECT
			`id`,
			`username`,
			`birthday`,
			`sex`,
			`address`
		FROM
			`USER`
		WHERE
			username like '%${value}%'
	</select>
	
	<!-- 	新增用户信息 -->
	<insert id="insertUser" parameterType="com.itheima.mybatis.pojo.User">
		INSERT INTO USER
		            (`username`,
		             `birthday`,
		             `sex`,
		             `address`)
		VALUES (#{username},
		        #{birthday},
		        #{sex},
		        #{address})
	</insert>
</mapper>